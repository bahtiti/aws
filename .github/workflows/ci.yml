# CI/CD is handled by Jenkins — see Jenkinsfile and jenkins/README.md
# This file is intentionally empty; GitHub Actions are NOT used for CI/CD.
#
# Jenkins pipelines:
#   Jenkinsfile        — main pipeline (quality gates + Lambda deploy + release)
#   Jenkinsfile.pr     — PR validation pipeline (triggered via GitHub webhook)
#
# Jenkins required status check reported back to GitHub:
#   Context name: "Jenkins CI/CD"
#   Configure this in: Settings → Branches → Require status checks
name: noop
on: []
jobs: {}
