# Drift detection is handled by a scheduled Jenkins job.
# Configure a Jenkins job on a cron trigger pointing to Jenkinsfile
# with DRIFT_ONLY=true parameter — see jenkins/README.md#drift-detection.
name: noop
on: []
jobs: {}
