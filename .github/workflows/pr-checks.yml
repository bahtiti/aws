# PR quality gates (title, description, branch policy, lint, security)
# are enforced by Jenkins — see Jenkinsfile stages prefixed 'PR:'.
#
# The only GitHub-native check kept here is auto-labeling (auto-label.yml).
# This file is intentionally empty.
name: noop
on: []
jobs: {}
